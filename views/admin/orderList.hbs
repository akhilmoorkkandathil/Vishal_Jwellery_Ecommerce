<hr class="my-5" />

              <!-- Hoverable Table rows -->
              <div class="card mx-4">
                <h5 class="card-header">Orders</h5>
                <div class="table-responsive text-nowrap">
                  
                  {{#if orders}}
                  <table class="table table-hover">
                    <thead>
                      <tr>
                        <th>OrderId</th>
                        <th>Username</th>
                        <th>Price</th>
                        <th>Address</th>
                        <th>Payment Methode</th>
                        <th>Status</th>
                        <th>OrderDate</th>
                        <th>Actions</th>
                      </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                      {{#each orders}}
                      <tr>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>{{this.orderId}}</strong></td>
                        <td>{{this.userName}}</td>
                        <td>{{this.totalPrice}}</td>
                        <td>
                          {{this.shippingAddress.fname}} {{this.shippingAddress.lname}}<br>
                          {{this.shippingAddress.locality}}, {{this.shippingAddress.city}}<br>
                          Phone: {{this.shippingAddress.phone}},<br> 
                          Alt Phone: {{this.shippingAddress.altphone}}<br>
                          Pincode: {{this.shippingAddress.pincode}}, <br>
                          Landmark: {{this.shippingAddress.landmark}}
                        </td>
                        <td>{{this.paymentMethod}}</td>
                        <td><span class="badge bg-label-primary me-1">{{this.status}}</span></td>
                        <td>{{this.createdAt}}</td>
                        <td>
                            <div class="dropdown">
                            <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                              <i class="bx bx-dots-vertical-rounded"></i>
                            </button>
                            <div class="dropdown-menu">
                              <a class="dropdown-item" href="shipped/{{this.orderId}}"
                                ><i class="bx bx-edit-alt me-1"></i> {{this.status}}</a
                              >
                             
                              
                              <a class="dropdown-item" href=""  onclick="return confirm('Do you want to delete {{this.name}}?')"
                                ><i class="bx bx-trash me-1"></i> View</a
                              >
                            </div>
                      </tr>
                      {{/each}}
                    </tbody>
                  </table>
                  {{else}}
                  <h5 class="ml-5">No Orders Yet!</h5>
                  {{/if}}
                </div>
              </div>
              <!--/ Hoverable Table rows -->

              <hr class="my-5" />