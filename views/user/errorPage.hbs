<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error 404 - Page Not Found</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            color: #343a40;
            text-align: center;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .error-container {
            max-width: 400px;
            margin: auto;
        }

        .error-heading {
            font-size: 5em;
            margin-bottom: 10px;
            color: #dc3545;
        }

        .error-message {
            font-size: 1.5em;
            margin-bottom: 20px;
        }

        .back-to-login-btn {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            font-size: 1.2em;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .back-to-login-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-heading">{{statusCode}}</div>
        <div class="error-message">
            {{#if statusCode}}
                {{#if statusMessage}}
                    {{statusMessage}}
                {{else}}
                    Oops! An error occurred.
                {{/if}}
            {{else}}
                Oops! The page you are looking for does not exist.
            {{/if}}
        </div>
        <a href="/" class="back-to-login-btn">Back to Home</a>
    </div>
</body>
</html>
